name: flutter_ci_cli
description: A CLI tool to automatically setup CI/CD for Flutter projects.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.18.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  args:       # For CLI arguments
  http:       # For GitHub API requests
  yaml:       # For YAML generation
  path:       # File path handling
  dotenv: 

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0


executables:
  flutter_ci_cli: flutter_ci_cli
